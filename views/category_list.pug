extends layout

block content
  h1= title

  ul
    each category in category_list
      li
        a(href=`/catalog/category/${category._id}`)= category.name 

  a.btn.btn-primary(href="/catalog/category/create") Create New Category
