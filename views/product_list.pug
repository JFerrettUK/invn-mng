extends layout

block content
  h1= title

  ul
    each product in product_list
      li
        a(href=`/catalog/product/${product._id}`)= product.name + ' - ' + product.category.name + ' (Price: ' + product.price.toFixed(2) + ')'

  a.btn.btn-primary(href="/catalog/product/create") Create New Product
